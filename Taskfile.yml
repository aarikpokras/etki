version: '3'

tasks:
  default:
    desc: "Set up ETkI for use"
    cmds:
      - mv src/* .
      - rm *.md
      - rm LICENSE
      - rm -rf tests
      - rm pyproject.toml
  clean:
    desc: "Remove everything that's not library files"
    cmds:
      - rm Makefile
      - rm Taskfile.yml
